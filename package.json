{
  "name": "botmsg",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-types": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply botmsg --local",
    "db:migrate:prod": "wrangler d1 migrations apply botmsg --remote"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
